# 云函数与Java接口对照表

## 总览

| 序号 | 云函数 | Java接口 | 方法 | 优先级 |
|------|--------|----------|------|--------|
| 1 | login | /user/login | POST | P0 |
| 2 | update-user-info | /user/info | PUT | P0 |
| 3 | update-user-avatar | /file/avatar | POST | P1 |
| 4 | update-user-level | /user/level | PUT | P0 |
| 5 | create-activity | /activity | POST | P0 |
| 6 | get-activities | /activity/list | GET | P0 |
| 7 | get-activity-detail | /activity/{id} | GET | P0 |
| 8 | delete-activity | /activity/{id} | DELETE | P1 |
| 9 | update-activity-status | /activity/{id}/status | PUT | P1 |
| 10 | join-activity | /registration/join | POST | P0 |
| 11 | cancel-registration | /registration/cancel | POST | P0 |
| 12 | handle-cancel-request | /registration/handle-cancel | POST | P1 |
| 13 | start-grouping | /match/grouping | POST | P0 |
| 14 | start-match | /match/start | POST | P1 |
| 15 | submit-score | /match/score | POST | P0 |
| 16 | confirm-score | /match/confirm-score | POST | P0 |
| 17 | get-match-detail | /match/{id} | GET | P1 |
| 18 | set-team-name | /team/{id}/name | PUT | P2 |
| 19 | start-challenge | /challenge/start | POST | P1 |
| 20 | get-challenge-data | /challenge/{activityId} | GET | P1 |
| 21 | create-challenge-match | /challenge/start | POST | P1 |
| 22 | start-final | /challenge/final | POST | P1 |
| 23 | get-rankings | /ranking/{activityId} | GET | P1 |
| 24 | ai-chat | /ai/chat | POST | P1 |
| 25 | ai-history | /ai/history | GET/DELETE | P2 |
| 26 | get-rules | /rules | GET | P2 |
| 27 | init-rules | (管理后台) | - | P2 |
| 28 | clear-database | (管理后台) | - | P2 |

---

## 详细对照

### 1. 用户模块

#### login → POST /user/login

**云函数代码位置**: `cloudfunctions/login/index.js`

**云函数参数**:
```javascript
{
  code: String  // 微信登录code
}
```

**云函数返回**:
```javascript
{
  success: true,
  data: {
    openid: String,
    nickname: String,
    avatar: String,
    level: Number
  }
}
```

**Java接口**:
```java
@PostMapping("/user/login")
public Result<UserVO> login(@RequestBody LoginRequest request) {
    // 1. 使用code调用微信接口获取openid
    // 2. 查询或创建用户
    // 3. 生成JWT Token
    // 4. 返回用户信息和Token
}
```

**Java DTO**:
```java
@Data
public class LoginRequest {
    private String code;
}

@Data
public class LoginResponse {
    private String token;
    private UserVO userInfo;
}
```

---

#### update-user-info → PUT /user/info

**云函数代码位置**: `cloudfunctions/update-user-info/index.js`

**云函数参数**:
```javascript
{
  nickname: String,
  avatar: String
}
```

**Java接口**:
```java
@PutMapping("/user/info")
public Result<Void> updateUserInfo(@RequestBody UpdateUserRequest request) {
    // 更新当前登录用户的昵称和头像
}
```

---

#### update-user-level → PUT /user/level

**云函数代码位置**: `cloudfunctions/update-user-level/index.js`

**云函数参数**:
```javascript
{
  activityId: String,
  level: Number
}
```

**Java接口**:
```java
@PutMapping("/user/level")
public Result<Void> updateLevel(@RequestBody UpdateLevelRequest request) {
    // 更新用户在特定活动中的等级
}
```

---

### 2. 活动模块

#### create-activity → POST /activity

**云函数代码位置**: `cloudfunctions/create-activity/index.js`

**云函数参数**:
```javascript
{
  name: String,
  time: Number,      // 时间戳
  location: String,
  latitude: Number,
  longitude: Number,
  type: String       // singles/doubles/fixed-doubles
}
```

**Java接口**:
```java
@PostMapping("/activity")
public Result<Long> createActivity(@RequestBody ActivityCreateRequest request) {
    // 1. 验证参数
    // 2. 创建活动记录
    // 3. 返回活动ID
}
```

---

#### get-activities → GET /activity/list

**云函数代码位置**: `cloudfunctions/get-activities/index.js`

**云函数参数**:
```javascript
{
  type: String,      // organized/joined/available/unavailable/search
  keyword: String    // 搜索关键词(可选)
}
```

**Java接口**:
```java
@GetMapping("/activity/list")
public Result<List<ActivityVO>> getActivities(
    @RequestParam String type,
    @RequestParam(required = false) String keyword) {
    // 根据type查询不同类型的活动列表
}
```

---

#### get-activity-detail → GET /activity/{id}

**云函数代码位置**: `cloudfunctions/get-activity-detail/index.js`

**云函数参数**:
```javascript
{
  activityId: String,
  openid: String
}
```

**Java接口**:
```java
@GetMapping("/activity/{id}")
public Result<ActivityDetailVO> getActivityDetail(@PathVariable Long id) {
    // 返回活动详情，包括报名列表、比赛列表等
}
```

**返回数据结构**:
```java
@Data
public class ActivityDetailVO {
    private ActivityVO activity;
    private List<RegistrationVO> registrations;
    private List<MatchVO> matches;
    private RegistrationVO userRegistration;
    private Boolean isOrganizer;
    private Integer pendingCancelCount;
}
```

---

### 3. 报名模块

#### join-activity → POST /registration/join

**云函数代码位置**: `cloudfunctions/join-activity/index.js`

**云函数参数**:
```javascript
{
  activityId: String,
  activityName: String,
  nickname: String,
  level: Number,
  avatar: String,
  partnerId: String
}
```

**Java接口**:
```java
@PostMapping("/registration/join")
public Result<Long> joinActivity(@RequestBody JoinActivityRequest request) {
    // 1. 验证活动状态
    // 2. 验证人数
    // 3. 验证是否重复报名
    // 4. 创建报名记录
    // 5. 更新活动人数
}
```

**业务规则**:
- 活动状态必须为 registering
- 人数不能超过 max_players
- 不能重复报名
- 固搭模式需要选择搭档

---

#### cancel-registration → POST /registration/cancel

**云函数代码位置**: `cloudfunctions/cancel-registration/index.js`

**云函数参数**:
```javascript
{
  activityId: String,
  activityName: String,
  reason: String
}
```

**Java接口**:
```java
@PostMapping("/registration/cancel")
public Result<Void> cancelRegistration(@RequestBody CancelRegistrationRequest request) {
    // 1. 查找报名记录
    // 2. 设置取消状态为 pending
    // 3. 等待组织者审批
}
```

---

### 4. 比赛模块

#### start-grouping → POST /match/grouping

**云函数代码位置**: `cloudfunctions/start-grouping/index.js`

**云函数参数**:
```javascript
{
  activityId: String
}
```

**Java接口**:
```java
@PostMapping("/match/grouping")
public Result<GroupingResult> startGrouping(@RequestBody ActivityRequest request) {
    // 1. 验证活动状态
    // 2. 获取所有报名用户
    // 3. 执行分组算法
    // 4. 创建队伍记录
    // 5. 创建比赛记录
    // 6. 更新活动状态
}
```

**分组算法**:
```java
public class GroupingAlgorithm {
    /**
     * 均衡分组
     * 1. 按等级降序排列
     * 2. 最高+最低配对
     * 3. 重复直到分配完毕
     */
    public static List<Team> balanceGroup(List<Registration> registrations) {
        registrations.sort((a, b) -> b.getLevel() - a.getLevel());

        List<Team> teams = new ArrayList<>();
        int left = 0, right = registrations.size() - 1;

        while (left < right) {
            Team team = new Team();
            List<Long> members = new ArrayList<>();
            members.add(registrations.get(left).getUserId());
            members.add(registrations.get(right).getUserId());
            team.setMembers(members);
            teams.add(team);
            left++;
            right--;
        }

        return teams;
    }
}
```

---

#### submit-score → POST /match/score

**云函数代码位置**: `cloudfunctions/submit-score/index.js`

**云函数参数**:
```javascript
{
  activityId: String,
  matchId: String,
  team1Score: Number,
  team2Score: Number
}
```

**Java接口**:
```java
@PostMapping("/match/score")
public Result<Void> submitScore(@RequestBody ScoreSubmitRequest request) {
    // 1. 验证比赛状态
    // 2. 验证比分范围(0-30)
    // 3. 验证记分权限
    // 4. 更新比赛比分
    // 5. 计算积分
    // 6. 保存积分记录
}
```

**积分计算规则**:
```java
public class ScoreCalculator {
    /**
     * 计算积分
     */
    public static int calculate(int myScore, int opponentScore, String round) {
        switch (round) {
            case "group":
                // 小组赛：胜者+分差，败者-分差
                return myScore - opponentScore;
            case "challenge":
                // 挑战赛：胜者+10，败者-10
                return myScore > opponentScore ? 10 : -10;
            case "final":
                // 决赛：胜者+15，败者-15
                return myScore > opponentScore ? 15 : -15;
            default:
                return 0;
        }
    }
}
```

---

#### confirm-score → POST /match/confirm-score

**云函数代码位置**: `cloudfunctions/confirm-score/index.js`

**云函数参数**:
```javascript
{
  activityId: String,
  matchId: String,
  confirmed: Boolean
}
```

**Java接口**:
```java
@PostMapping("/match/confirm-score")
public Result<Void> confirmScore(@RequestBody ScoreConfirmRequest request) {
    // 1. 验证比赛状态为 confirming
    // 2. 更新确认状态
    // 3. 两队都确认后，更新比赛状态为 confirmed
    // 4. 计算并保存积分
}
```

---

### 5. 挑战赛模块

#### start-challenge → POST /challenge/start

**云函数代码位置**: `cloudfunctions/start-challenge/index.js`

**Java接口**:
```java
@PostMapping("/challenge/start")
public Result<Void> startChallenge(@RequestBody ActivityRequest request) {
    // 1. 验证所有小组赛已完成
    // 2. 计算积分排名
    // 3. 找出积分最高的2人作为晋级队
    // 4. 其他队伍标记为淘汰
    // 5. 允许淘汰者自由组队
    // 6. 创建挑战赛比赛
}
```

---

#### start-final → POST /challenge/final

**云函数代码位置**: `cloudfunctions/start-final/index.js`

**Java接口**:
```java
@PostMapping("/challenge/final")
public Result<Void> startFinal(@RequestBody ActivityRequest request) {
    // 1. 验证所有挑战赛已完成
    // 2. 创建决赛比赛
    // 3. 更新活动状态为 final
}
```

---

### 6. AI模块

#### ai-chat → POST /ai/chat

**云函数代码位置**: `cloudfunctions/ai-chat/index.js`

**云函数参数**:
```javascript
{
  message: String,
  action_result: String  // 操作结果反馈(可选)
}
```

**Java接口**:
```java
@PostMapping("/ai/chat")
public Result<AiChatResponse> chat(@RequestBody AiChatRequest request) {
    // 1. 获取用户上下文
    // 2. 获取历史消息
    // 3. 构建prompt
    // 4. 调用AI API
    // 5. 解析响应
    // 6. 保存消息
}
```

**AI API调用**:
```java
@Service
public class AiService {
    @Value("${ai.api-key}")
    private String apiKey;

    @Value("${ai.api-url}")
    private String apiUrl;

    public String chat(List<Message> messages) {
        // 调用小米Mimo API
        // 或其他AI服务
    }
}
```

---

### 7. 规则模块

#### get-rules → GET /rules

**云函数代码位置**: `cloudfunctions/get-rules/index.js`

**Java接口**:
```java
@GetMapping("/rules")
public Result<List<RulesSection>> getRules() {
    // 从数据库获取规则
    // 如果没有，返回默认规则
}
```

---

## 数据库集合与表对照

| 云数据库集合 | MySQL表 | 说明 |
|-------------|---------|------|
| users | users | 用户表 |
| activities | activities | 活动表 |
| registrations | registrations | 报名表 |
| teams | teams | 队伍表 |
| matches | matches | 比赛表 |
| scores | scores | 积分表 |
| ai_messages | ai_messages | AI消息表 |
| announcements | rules | 规则表 |

---

## 字段类型对照

| 云数据库类型 | MySQL类型 | 说明 |
|-------------|-----------|------|
| String | VARCHAR/TEXT | 字符串 |
| Number | INT/BIGINT/DECIMAL | 数字 |
| Boolean | TINYINT(1) | 布尔值 |
| Date | DATETIME | 日期时间 |
| Array | JSON | 数组 |
| Object | JSON | 对象 |
| _id | id (BIGINT) | 主键 |
| _openid | openid (VARCHAR) | 用户标识 |
